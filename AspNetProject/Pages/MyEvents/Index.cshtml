@page
@model AspNetProject.Pages.MyEvents.IndexModel

@{ ViewData["Title"] = "Index"; }

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Title
            </th>
            <th>
                Description
            </th>
            <th>
                Place
            </th>
            <th>
                Adress
            </th>
            <th>
                Date
            </th>
            <th>
                TicketsAvailable
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.AttendeeEvent)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Event.Title)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Event.Description)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Event.Place)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Event.Adress)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Event.Date)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Event.TicketsAvailable)
    </td>
    <td>
        <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
        <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
    </td>
</tr>}
    </tbody>
</table>
